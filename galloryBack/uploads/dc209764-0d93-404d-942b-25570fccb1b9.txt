Service:


public List<File> getAllFilesOfFolder(Long folderId){

    Folder folder = this.folderRepository.findByFolderId(folderId);

    List <File> files = (List <File>)folder.getFiles();
    return files;

}

Repository

@Query("SELECT f FROM file p WHERE f.name LIKE %?1%"
        + " OR f.extention LIKE %?1%"
        + " OR f.type LIKE %?1%"
        + " OR CONCAT(f.size, '') LIKE %?1%")
 List<File> search(String keyword);


